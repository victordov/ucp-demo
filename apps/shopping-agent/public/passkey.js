/* MOVED: the WebAuthn/SPC passkey client now lives on the WALLET origin —
 * apps/credentials-provider/public/passkey-client.js — and runs inside the
 * sandboxed payment-sheet iframe (sheet.html). The agent page no longer
 * performs any passkey ceremony itself. This file is intentionally empty
 * and no longer loaded by index.html. */
